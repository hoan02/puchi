# Suggested Commands

- `npm run dev` — Start development server (Next.js, port 4000)
- `npm run build` — Build for production
- `npm run start` — Start production server
- `npm run lint` — Run ESLint

## Utilities
- Standard Windows commands (e.g., `dir`, `cd`, `findstr`)
- Git for version control

No test or format scripts found. If you add tests or formatting tools, update this list.